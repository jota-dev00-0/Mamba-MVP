// server.ejs

const express = require('express');
const app = express();

app.use(express.json());
app.use(express.urlencoded({ extended: true }));

app.get('/', (req, res) => {
  res.send('Bem-vindo à API <%= projectName %>!');
});

const PORT = <%= port %>;
app.listen(PORT, () => {
  console.log('Servidor rodando na porta ' + PORT);
});
